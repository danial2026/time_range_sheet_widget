name: time_range_sheet_widget
description: A highly customizable, reusable time range picker bottom sheet widget for Flutter with extensive styling options and responsive behavior.
version: 0.0.3
homepage: https://github.com/danial2026/time_range_sheet_widget

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
